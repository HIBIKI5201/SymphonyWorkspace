---
name: uloop-launch
description: "Launch or restart Unity Editor. Use only when Unity is not running or stays frozen after retries — not as a health check after a failed command; it brings the Unity window to the foreground as a side effect."
---

<!-- Generated by scripts/sync_agent_skill_locators.py. Edit the canonical skill under .agents/skills instead. -->

# Skill locator

Before taking task actions, read `.agents/skills/uloop-launch/SKILL.md` completely and follow it as
the authoritative skill instructions. Resolve every relative path mentioned by
that file against `.agents/skills/uloop-launch/`, not this locator directory.
