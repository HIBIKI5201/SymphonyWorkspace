# B6. テストとサンプルの追随

テスト配置、実行結果、今回の確定問題に対する回帰テストの有無を確認した。

## 実行結果

| モード | 1回目 | 2回目 | Skipped |
| --- | ---: | ---: | ---: |
| EditMode | 752 / 752成功 | 752 / 752成功 | 0 |
| PlayMode | 21 / 21成功 | 21 / 21成功 | 0 |

## 【要検証・高】ServiceLocateComponentの所有者違い解除を覆うテストがない

**場所**: [ServiceLocateComponent.cs:79](../../../Assets/SymphonyFrameWork/Runtime/Service/ServiceLocator/ServiceLocateComponent.cs#L79)

同型のAをComponentが登録後、Aが別経路で解除されBが登録された状態でComponentを無効化するケースがない。この経路ではBが解除される。

**修正方針**: 型だけでなくインスタンス同一性を条件にした回帰テストを追加する。

## 【要検証】PlayModeテストが3ファイルに集中している

**場所**: `Assets/SymphonyFrameWork/Tests/Runtime/`

21件はAwaitable、Pause Awaitable、SaveStore Awaitableのみ。Scene Loader、Scene Block、Audio、Service LocatorのUnityライフサイクルとPlay Mode往復は専用テストがない。前回から件数は増えたが対象領域は残る。

**修正方針**: まずOrchestratorの2往復、ServiceLocateComponent、Sceneロードキャンセルを優先する。

前回のSample配置問題は`Samples~/`移動とpackage.jsonの`samples`定義で解消済み。

