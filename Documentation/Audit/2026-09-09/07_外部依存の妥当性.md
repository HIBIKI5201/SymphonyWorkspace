# 07. 外部依存の妥当性

`package.json`、asmdef、実際のAPI参照を突き合わせた。

## 【設計指摘】Unity下限が実際の検証環境より広い

**場所**: [package.json](../../../Assets/SymphonyFrameWork/package.json)

`unity`は`6000.0`だが、開発と検証は6000.3.10f1で、Main Toolbarなど6000.3系を前提とするEditor機能がある。前回のAddressables 1.21.19と2.9.1の不一致は解消済み。

**修正方針**: 6000.0での実コンパイルをCIへ加えるか、サポート下限を実検証範囲へ合わせる。

