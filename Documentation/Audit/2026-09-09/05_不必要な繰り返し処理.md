# 05. 不必要な繰り返し処理

RuntimeのLINQと毎フレーム経路を確認した。

## 【設計指摘】HUD追加テキストが更新ごとにStringBuilderを生成する

**場所**: [SymphonyHUDDrawer.cs:137](../../../Assets/SymphonyFrameWork/Runtime/Debug/DebugHUD/Internal/SymphonyHUDDrawer.cs#L137)

`GetExtraText`は表示更新ごとに新しい`StringBuilder`を作り、直後に既存builderへ追加する。前回から残存。

**修正方針**: 呼び出し元のbuilderへ直接追記する。登録件数が少なくHUD限定なので優先度は低い。

